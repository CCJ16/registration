'use strict';

describe('ccj16reg.registration module', function() {
  beforeEach(module('ccj16reg.registration'));

  describe('version service', function() {
    it('should return current version', inject(function(registration) {
      
    }));
  });
});
